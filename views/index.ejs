<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="css/style.css">
</head>


<body>
    <h1 class="heading">
        <%=kindofday %>
    </h1>

    <form action="/" method="post">
        <input type="text" placeholder="Enter..." name="newItem" class="adding">
        <button type="submit" name="sub_btn" class="submit">Add</button>
    </form>

    <ul class="list-cover">
        <!-- using foreach loop for rendering the data; -->
        <% lists.forEach(function(item){%>
            <form action="/delete" method="post">
                <li>
                    <input type="checkbox" onclick="this.form.submit()" value="<%=item._id%>" name="checkbox"
                        class="checkbox">
                    <%=item.name%>
                </li>
            </form>
            <% }) %>
    </ul>




</body>

</html>